/** @type {import('next').NextConfig} */
const nextConfig = {
  // The 17 theme specimens are fully self-contained static HTML files served
  // straight from /public/themes. Nothing to transform — Next just serves them.
  reactStrictMode: true,
};

export default nextConfig;
